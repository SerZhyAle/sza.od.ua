# Google Drive pointers
*.gsite
*.gdoc

# System / OS files
Thumbs.db
Desktop.ini
.DS_Store

# Local research scratch (downloaded site copies, palette mockups)
.research/

# Secrets and signing material - pre-empt the leak, never catch it afterwards.
*.pfx
*.snk
*.jks
*.keystore
.env
*token*
*secret*

# Local editor state - a per-machine tint, not a project setting, and `git add .` in deploy.bat
# would publish it with the site.
.vscode/
